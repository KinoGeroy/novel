import React from 'react';
import * as style from './statsField.module.scss';

const StatsField = () => {
    return (
        <div className={style.statsField}>
            stats
        </div>
    );
};

export default StatsField;